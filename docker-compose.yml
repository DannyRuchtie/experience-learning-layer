services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: ell
      POSTGRES_PASSWORD: ell
      POSTGRES_DB: ell
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
